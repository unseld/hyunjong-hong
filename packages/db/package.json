{
  "name": "@acme/db",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "prisma": {"seed": "tsx prisma/seed.ts"},
  "scripts": {
    "migrate": "prisma migrate dev --name init",
    "generate": "prisma generate",
    "seed": "prisma db seed",
    "build": "tsc -p tsconfig.json",
    "lint": "echo db lint ok"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
